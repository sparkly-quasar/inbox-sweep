{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window. The app only needs its own commands; no filesystem, shell or HTTP plugin access is granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
