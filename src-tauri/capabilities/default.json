{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window. The app grants only its own commands plus the updater and a restart \u2014 no filesystem, shell or arbitrary HTTP access.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "updater:default",
    "process:allow-restart"
  ]
}
